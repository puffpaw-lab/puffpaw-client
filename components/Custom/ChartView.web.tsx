import { CustomChartProps } from "@/constants/Props";
import React from "react";
import { View } from "react-native";

export const HomeChartView = ({ style, children }: CustomChartProps) => {
  return <View></View>;
};
